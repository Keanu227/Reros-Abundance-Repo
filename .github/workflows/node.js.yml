cd C:\path\to\Reros-Abundance-Repo
dir              # confirm package.json and server.js are listed
npm install      # installs dependencies
npm run dev      # starts the server
// Print your mantra when the server starts
console.log(`
Clarity in code, trust in design,
Every line builds freedom, every step defines.
I start with focus, I run with intent,
My app is my vision, my energy well-spent.
`);

// Import Express
const express = require("express");
const app = express();

// Define a simple route
app.get("/", (req, res) => {
  res.send("Welcome to Reros Abundance Backend 🚀");
});

// Start the server
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Server running on http://localhost:${PORT}`);
});

# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test
